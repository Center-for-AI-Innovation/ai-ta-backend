# Backend .env.template — mirror of the kept-keys set in `.env`.
# See /Users/rmp6/Documents/cropwizard/cleaned.env for the canonical superset
# used during local development.

# =============================================================================
# OPENAI & LLM
# =============================================================================
OPENAI_API_KEY=
VLADS_OPENAI_KEY=${OPENAI_API_KEY}
NCSA_HOSTED_API_KEY=
OLLAMA_SERVER_URL=

# =============================================================================
# EMBEDDINGS
# =============================================================================
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B
EMBEDDING_API_BASE=
# Comma-separated allow-list (lowercased) for `embedding_config.provider`.
# Enforced by `_resolve_embedding_client` (ValueError on unknown providers).
# Mirrored on the frontend by the same env var.
ALLOWED_EMBEDDING_PROVIDERS=openai,ollama
# QWEN_QUERY_INSTRUCTION=    # optional override

# =============================================================================
# REDIS
# =============================================================================
INGEST_REDIS_HOST=
INGEST_REDIS_PORT=
INGEST_REDIS_PASSWORD=
REDIS_URL=
REDIS_PASSWORD=

# =============================================================================
# SERVICE PORTS / URLS
# =============================================================================
FRONTEND_PORT=
FLASK_PORT=
RAILWAY_URL=
INGEST_URL=

# =============================================================================
# OBJECT STORAGE
# =============================================================================
LOCAL_MINIO=
MINIO_URL=
MINIO_ENDPOINT=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=

# =============================================================================
# POSTGRES
# =============================================================================
POSTGRES_PASSWORD=
POSTGRES_ENDPOINT=
POSTGRES_PORT=
POSTGRES_DATABASE=
POSTGRES_USERNAME=

KEYCLOAK_DB_USERNAME=
KEYCLOAK_DB_PASSWORD=
KEYCLOAK_DB_ENDPOINT=
KEYCLOAK_DB_PORT=
KEYCLOAK_DB_DATABASE=

# =============================================================================
# QDRANT
# =============================================================================
QDRANT_API_KEY=
QDRANT_COLLECTION_NAME=
QDRANT_URL=
QDRANT_PORT=

# =============================================================================
# AUTH / KEYCLOAK
# =============================================================================
NEXT_PUBLIC_SIGNING_KEY=
KEYCLOAK_ADMIN_USERNAME=
KEYCLOAK_ADMIN_PASSWORD=
KC_HOSTNAME=

# =============================================================================
# ENCRYPTION (must match frontend ENCRYPTION_MASTER_KEY)
# =============================================================================
# Generate with: python -c "import os, base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
ENCRYPTION_MASTER_KEY=

# =============================================================================
# OBSERVABILITY
# =============================================================================
POSTHOG_API_KEY=

# =============================================================================
# MISC INTEGRATIONS (optional)
# =============================================================================
CANVAS_ACCESS_TOKEN=
# SUPABASE_URL=         # used by utils/rerun_webcrawl_for_project.py + pubmed_extraction.py
# SUPABASE_API_KEY=
NOMIC_API_KEY=          # service/nomic_service.py:23
KV_REST_API_TOKEN=      # service/project_service.py:54

# =============================================================================
# EMAIL (SMTP) — utils/email/* and service/export_service.py
# =============================================================================
SES_HOST=
SES_PORT=
EMAIL_SENDER=
USERNAME_SMTP=
PASSWORD_SMTP=
